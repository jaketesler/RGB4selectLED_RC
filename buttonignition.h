// buttonignition.h //

void buttonignition1()
{
  //below is the button ignition
  //this seems to cause Switch3 to be activated all the time...must investigate further
  /*
      button1 = analogRead(BUTTON);
      Serial.print("setup: ");
      Serial.println(button1);
      
      button1 = analogRead(BUTTON);
      
      while(button1 < 5)
      {
        button1 = analogRead(BUTTON);
        Serial.print("<500, i0, otm0: ");
        Serial.println(button1);
        button1 = analogRead(BUTTON);
        delay(1500);
        
        button1 = analogRead(BUTTON);
        tone(BUZZ, 98, 250);
        digitalWrite(L, HIGH);
        delay(251);
        
        button1 = analogRead(BUTTON);
        digitalWrite(L, LOW);
        delay(250);
        
        button1 = analogRead(BUTTON);
        tone(BUZZ, 98, 250);
        digitalWrite(L, HIGH);
        delay(251);
        
        button1 = analogRead(BUTTON);
        digitalWrite(L, LOW);
        delay(250);
        
        button1 = analogRead(BUTTON);
        tone(BUZZ, 98, 250);
        digitalWrite(L, HIGH);
        delay(251);
        
        button1 = analogRead(BUTTON);
        digitalWrite(L, LOW);
        
        button1 = analogRead(BUTTON);
        delay(1750);
      }
     
      Serial.print(">500, i0, otm0: ");
      Serial.println(button1);
        
      button1 = analogRead(BUTTON);
      ignition = 1 ;
      delay(400);
      button1 = analogRead(BUTTON);
      
      //attempt to correct S3 ON all the time error
      switch1 = 0; 
      switch2 = 0;
      switch3 = 0;
      switch4 = 0;
      
      */
}
